# This script will allow you to run all the shiny apps:
# "shiny_app_1"
# "shiny_app_2"
# "shiny_app_3"

# To run each app:
	# 1) install.packages("shiny", dependencies = TRUE)
	# 2) library(shiny)
	# 3) setwd("name_of_app_folder")
	# 4) execute runApp()
	# Note: ggplot2 needs to be installed

## Runable code

library(shiny)

setwd("")

runApp()



